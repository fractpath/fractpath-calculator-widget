# WGT-050 — Contract versioning + deprecation policy (no silent mutation)

## Objective
Codify breaking-change protocol for widget interface + serialized payloads.
Includes: version bumps, changelog, compatibility window, coordinated releases.
